package mg.rosii.management.user;

/**
 * Application roles. V1 has exactly one: the patronne.
 * Employees do not have accounts in V1; further roles will be introduced
 * only when the business requires them.
 */
public enum Role {
    PATRONNE
}
